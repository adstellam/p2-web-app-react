import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';

export default function SettingADetails() {

    const navigate = useNavigate();

    return (
        <div>SettingADetails . . .</div>
    );
}